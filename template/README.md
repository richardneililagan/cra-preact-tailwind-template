{{ name }}
===

## Usage

```bash
# :: install dependencies
yarn install

# :: start dev server + hot reloading (PORT=3000 by default)
yarn start

# :: build for production
yarn build

# :: run tests + watch
yarn test

# :: lint code files
yarn lint
```
